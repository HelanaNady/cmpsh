#include <unistd.h>
#include <stdio.h>
#include <stdbool.h>
#include <string.h>
#include "utils.h"

int main(int argc, char** argv) {

    // check if there are arguments first
    // command?

    cmp_shell_loop();    

    return 0;
}